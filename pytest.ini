[pytest]
# Restrict discovery strictly to the tests directory
testpaths = tests

# Exclude debug artifacts with non-UTF8 content from test discovery
norecursedirs = debug .git .venv env build dist

# Explicit ignore just in case
addopts = --ignore=debug

# (Optional) You can also further filter tests:
# addopts = -k 'not legacy'
