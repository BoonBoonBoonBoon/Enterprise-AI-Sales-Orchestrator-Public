langchain==1.0.4
# Align with langchain 1.0.x to keep deepagents middleware imports available
langchain-core==1.0.7
langchain-openai==1.0.1
langgraph>=1.0.2,<1.1.0
supabase==2.10.0
python-dotenv
redis==5.0.8
deepagents==0.2.5
prometheus-client
requests  # For Loki log push
pydantic>=2.0.0  # Type-safe schemas and validation

# Email ingress (Tier-0)
google-api-python-client>=2.110.0
google-auth>=2.25.0
google-auth-oauthlib>=1.2.0

# Webhook ingress (FastAPI)
fastapi>=0.110.0
uvicorn>=0.27.0

# LLM providers for copywriter agent
openai>=1.0.0  # OpenAI API (GPT-4, GPT-3.5)
anthropic>=0.7.0  # Anthropic API (Claude 3+)

# OpenTelemetry for distributed tracing
opentelemetry-api
opentelemetry-sdk
opentelemetry-instrumentation-redis
opentelemetry-exporter-otlp
opentelemetry-exporter-jaeger

# Secrets Management (optional, install based on cloud provider)
# azure-keyvault-secrets>=4.7.0  # Azure Key Vault support
# azure-identity>=1.15.0  # Azure authentication
# boto3>=1.34.0  # AWS Secrets Manager support

# Testing and CI/CD
pytest>=7.4.0  # Test framework
pytest-cov>=4.1.0  # Coverage reporting
pytest-asyncio>=0.21.0  # Async test support
pytest-timeout>=2.1.0  # Test timeout enforcement
flake8>=6.1.0  # Code linting
mypy>=1.5.0  # Static type checking
