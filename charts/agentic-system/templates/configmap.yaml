apiVersion: v1
kind: ConfigMap
metadata:
  name: agentic-config
  namespace: {{ include "agentic.namespace" . }}

data:
{{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
{{- end }}
